"""
Standalone evaluation script for HAC-VT.
Purpose:
- Run lightweight model comparison
- Measure inference time
- Export a single master comparison table

IMPORTANT:
- This script does NOT train or recalibrate any model.
- It assumes models are already implemented and usable for inference.
"""

def main():
    print("HAC-VT evaluation script loaded successfully.")
    print("No training or calibration executed.")

if __name__ == "__main__":
    main()
